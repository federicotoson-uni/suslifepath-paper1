# SSCI toolchain — Python dependencies
# Install in a dedicated venv:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Versions pinned to the ones validated on 2026-06-09 (macOS 14 / Python 3.14).
# Bump conservatively; rerun the end-to-end test (ssci_orchestrator.py) after
# any change.

pyyaml>=6.0,<7
numpy>=1.20,<3
matplotlib>=3.5,<4       # figures (paper1_figures.py)
olca-ipc>=2.6,<3
olca-schema>=2.6,<3
